{
  "name": "subscription-curator-spike",
  "version": "1.0.0",
  "description": "YouTube Subscription Curator test harness with local JSON storage",
  "private": true,
  "workspaces": [
    "server",
    "src"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=src\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:frontend": "npm run dev --workspace=src",
    "install:all": "npm install && npm install --workspaces",
    "fit:run": "node scripts/batch.run.js --channels data/channels.json --labels data/labelbook.json --out data/fitting_results.csv --jsonl data/assignments.jsonl --anchors data/anchors.json --verbose=1",
    "fit:metrics": "node scripts/metrics.summary.js --jsonl data/assignments.jsonl --prev data/assignments.prev.jsonl --out data/metrics.json"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "node-fetch": "^3.3.2"
  }
}
